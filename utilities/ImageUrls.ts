export const GetSliderImage = (imageName: string): string => {
  return "http://shop-api.codeyad-project.ir/images/sliders/" + imageName;
};
export const GetCategoryImage = (imageName: string): string => {
  return "http://shop-api.codeyad-project.ir/images/categories/" + imageName;
};
export const GetBannerImage = (imageName: string): string => {
  return "http://shop-api.codeyad-project.ir/images/banners/" + imageName;
};
export const GetProductImage = (imageName: string): string => {
  return "http://shop-api.codeyad-project.ir/images/products/" + imageName;
};
export const GetUserAvatar = (imageName: string): string => {
  return "http://shop-api.codeyad-project.ir/images/users/avatar/" + imageName;
};
export const GetProductGalleryImage = (imageName: string): string => {
  return (
    "http://shop-api.codeyad-project.ir/images/products/gallery/" + imageName
  );
};
